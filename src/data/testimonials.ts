import type { Testimonial } from "@/types";

// No genuine testimonials supplied yet. Add real, attributable quotes here
// as { quote, author } — the homepage section that renders this stays
// hidden while the array is empty (see src/app/page.tsx).
export const testimonials: Testimonial[] = [];
