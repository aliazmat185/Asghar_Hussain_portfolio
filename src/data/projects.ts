import type { Project } from "@/types";

// No verified projects supplied yet. Add real, completed work here as
// { title, summary, platform } — the homepage section that renders this
// stays hidden while the array is empty (see src/app/page.tsx).
export const projects: Project[] = [];
